Phase 8C: Space Agent Workspace Workflow Library

You are working on Logan's Bazzite Local AI Operations Stack repo:

~/projects/gem

Goal:
Build the L7 Space Agent Workspace Workflow Library for future supervised workspace operations.

This is documentation-only. Do not run Space Agent tasks, do not edit Space Agent config, and do not grant Space Agent repo edit access.

Current state:
- Phase 8B memory workflow library is complete (8B.1, 8B.2, 8B.3)
- Phase 8B.4: Quality dry-run (future prompt)
- Phase 8B.5: Production promotion review (future prompt)
- Space Agent status: installed, manual UI only, OpenRouter working, local Gemma/Ollama working
- Space Agent config: ~/conf/onscreen-agent.yaml (real file, repo must NOT create or edit it)
- Space Agent state: ~/.config/space-agent/, ~/.local/share/space-agent/ (do not modify)
- Stage 3A remains canonical fallback for all memory operations
- Existing validators pass:
  - gemma-evals-status
  - gemma-evals-check
  - gemma-examples-check

Component roles:
- Space Agent: L7, manual UI/workspace layer only — no autonomous tasks
- Human: approval authority for all workspace operations
- RuVector: L6, prototype-only, no production promotion yet
- Stage 3A: L3, canonical deterministic fallback
- Gemma wrappers: L1-L2, advisory/RAG synthesis only
- OpenCode/Codex: L3, implementation work, repo edits (with human approval)
- Agent Zero/A0: L5, future supervised workflow orchestration only

Hard boundaries:
- Do not use sudo.
- Do not install packages.
- Do not download models.
- Do not pull new Ollama models.
- Do not modify Ollama config or model config.
- Do not start Agent Zero.
- Do not run A0 connector tasks.
- Do not spawn Agent Zero agents.
- Do not run Space Agent autonomous tasks.
- Do not let Space Agent edit ~/projects/gem (no repo edit access).
- Do not create or edit ~/conf/onscreen-agent.yaml (real Space Agent config file).
- Do not modify system/security/package/model/OpenCode/Agent Zero/Space Agent config.
- Do not ingest new memory in this phase.
- Do not promote RuVector to production memory.
- Do not allow autonomous memory ingestion.
- Do not ingest secrets, .env files, raw logs, browser data, private code, unredacted transcripts, cookies, Local Storage, Session Storage, Trust Tokens, or provider state.
- Do not write runtime logs/reports/state into unrelated project roots.

Tasks:

1. Read Space Agent docs:
   - docs/integrations/space-agent/SPACE_AGENT_INTEGRATION_PLAN.md
   - docs/integrations/space-agent/SPACE_AGENT_PHASE7C_WORKSPACE_AND_MODELS_REPORT.md
   - docs/integrations/space-agent/SPACE_AGENT_PHASE7E_PROVIDER_AND_LOCAL_AGENT_REPORT.md
   - docs/integrations/space-agent/SPACE_AGENT_PHASE7E1_PROVIDER_FINALIZATION.md
   - docs/architecture/COMPONENT_ROUTING_MATRIX.md
   - docs/architecture/DATA_FLOW_AND_STATE_MAP.md
   - docs/roadmap/ROADMAP.md

2. Create workspace workflow library:
   docs/workflows/space-agent/WORKSPACE_WORKFLOW_LIBRARY.md

   Include 3-5 L7 workflow categories:
   - Workspace task definition
   - Provider/model configuration (manual only, no repo edits)
   - Workspace state inspection (read-only)
   - Manual task execution (no autonomous tasks)
   - Workspace reporting (output to ~/offload/security-reports/manual/)

   For each workflow define:
   - Workflow name
   - Capability level (L7)
   - Purpose.
   - When to use.
   - Inputs.
   - Approved paths (Space Agent config/state paths, ~/offload/security-reports/manual/)
   - Denied paths (~/projects/gem edits, ~/conf/onscreen-agent.yaml edits, secrets, .env, private data)
   - Allowed components/tools (Space Agent UI manually, OpenCode for repo docs)
   - Forbidden actions (no autonomous tasks, no repo edits, no config edits)
   - Execution steps (all manual/human-supervised)
   - Validation steps.
   - Evidence artifacts (saved to ~/offload/security-reports/manual/)
   - Fallback path (Stage 3A for memory, human for workspace)
   - Stop conditions (autonomous task request, repo edit request, config edit request)
   - Human approval points (all workspace operations, all provider changes)

3. Create workspace templates:
   - docs/workflows/templates/space-agent-workspace-template.md
   - Template for workspace task reports
   - Template for provider configuration notes (manual, no secrets)

4. Create checklists:
   - docs/workflows/space-agent/WORKSPACE_CHECKLIST.md
   - Concise checklist for workspace operations
   - Include: approved paths check, denied paths check, no autonomous tasks, no repo edits, human approval captured

5. Update docs/roadmap/ROADMAP.md:
   Add or update Phase 8C:
   - status: completed once workflow library exists
   - deliverables:
     - WORKSPACE_WORKFLOW_LIBRARY.md
     - WORKSPACE_CHECKLIST.md
     - space-agent-workspace-template.md
   - note: documentation-only; Space Agent manual UI only; no autonomous tasks; no repo edits; ~/conf/onscreen-agent.yaml not modified

6. Optional update:
   If useful, update docs/architecture/COMPONENT_ROUTING_MATRIX.md with Space Agent L7 workflow references.

Validation:
Run from ~/projects/gem:

test -f docs/workflows/space-agent/WORKSPACE_WORKFLOW_LIBRARY.md
test -f docs/workflows/space-agent/WORKSPACE_CHECKLIST.md
test -f docs/workflows/templates/space-agent-workspace-template.md
gemma-evals-status
gemma-evals-check
gemma-examples-check
git status --short
git diff -- docs/workflows docs/roadmap/ROADMAP.md

Acceptance criteria:
- WORKSPACE_WORKFLOW_LIBRARY.md exists with 3-5 L7 workflow categories.
- WORKSPACE_CHECKLIST.md exists.
- space-agent-workspace-template.md exists.
- All workflows are L7 (Space Agent workspace operations).
- Approved paths explicit (Space Agent config/state, ~/offload/security-reports/manual/).
- Denied paths explicit (~/projects/gem edits, ~/conf/onscreen-agent.yaml, secrets).
- Space Agent is manual UI only — no autonomous tasks.
- ~/conf/onscreen-agent.yaml is NOT created or edited by repo.
- Space Agent does NOT have repo edit access.
- No autonomous tasks are defined or enabled.
- No system/security/package/model/OpenCode/Agent Zero/Space Agent config changes.
- Existing validators pass.
- Roadmap reflects Phase 8C completion.

Final response format:
- files created
- files modified
- workflows defined (list with L7 level)
- approved paths
- denied paths
- space agent scope (manual UI only yes/no)
- repo edit access (granted yes/no — must be NO)
- config edited (onscreen-agent.yaml modified yes/no — must be NO)
- validators run
- pass/fail results
- readiness decision
- recommended next prompt
