Phase 7E.1: Space Agent Local Gemma And Gemini Manual Retry Prompt

You are working on Logan's Bazzite Local AI Operations Stack repo:

~/projects/gem

Goal:
Guide and document a manual Space Agent UI retry for local Gemma/Ollama and Gemini without exposing secrets, editing Space Agent config directly, or allowing Space Agent to edit the repo.

Current state:
- Phase 7E report exists: docs/integrations/space-agent/SPACE_AGENT_PHASE7E_PROVIDER_AND_LOCAL_AGENT_REPORT.md
- Space Agent is running manually as Space-Agent-0.66-linux-x64.AppImage.
- OpenRouter has worked in the Space Agent UI with:
  - Endpoint: https://openrouter.ai/api/v1/chat/completions
  - Model: openrouter/free
- Local Ollama is running at http://127.0.0.1:11434.
- Local Ollama OpenAI-compatible /v1/models works.
- A tiny local chat-completions request returned an Ollama chat completion object for gemma4-e4b-bazzite:latest.
- Gemini native API did not work in the UI during the previous attempt.
- Local Gemma/Ollama did not work in the UI during the previous attempt.

Hard boundaries:
- Do not print API keys, provider secrets, cookies, Local Storage, Session Storage, Trust Tokens, tokens, browser data, raw logs, or private code.
- Do not read or cat Space Agent config files that may contain credentials or browser/session/provider state.
- Do not edit, create, or overwrite ~/conf/onscreen-agent.yaml.
- Do not edit Space Agent config directly.
- Do not let Space Agent edit ~/projects/gem.
- Do not grant Space Agent broad filesystem access.
- Do not run Space Agent autonomous tasks.
- Do not start Agent Zero.
- Do not use sudo.
- Do not install packages.
- Do not download models.
- Do not modify firewall, USBGuard, ClamAV, Lynis, rpm-ostree, systemd timers, package state, Ollama config, model config, OpenCode permissions, Agent Zero config, or Space Agent config.

Manual UI retry instructions for the user:

1. In Space Agent UI, retry local Gemma/Ollama using API/OpenAI-compatible mode:
   - Endpoint: http://127.0.0.1:11434/v1/chat/completions
   - Model: gemma4-e4b-bazzite:latest
   - API key: ollama
   - Prompt: Reply with only: local gemma ok
   - Keep the request tiny. Do not run autonomous tasks.

2. In Space Agent UI, retry Gemini using API/OpenAI-compatible mode:
   - Endpoint: https://generativelanguage.googleapis.com/v1beta/openai/chat/completions
   - Model: gemini-2.5-flash
   - API key: enter manually in the UI only; do not paste it into terminal or repo docs
   - Prompt: Reply with only: gemini ok
   - If flash fails and the UI error is non-secret, record the visible non-secret error text.

3. Optional Gemini second try only if the first Gemini retry fails for model availability:
   - Endpoint: https://generativelanguage.googleapis.com/v1beta/openai/chat/completions
   - Model: gemini-2.5-pro
   - API key: manually entered in UI only

Documentation task:
- Create or update a concise follow-up report under docs/integrations/space-agent/.
- Record only non-secret fields:
  - provider tested
  - endpoint URL
  - model name
  - success/failure
  - non-secret UI error text, if any
  - whether API keys were omitted from the repo
- Do not record actual API keys or provider-secret storage.

Validation:
Run from ~/projects/gem:

test -f docs/integrations/space-agent/SPACE_AGENT_PHASE7E_PROVIDER_AND_LOCAL_AGENT_REPORT.md
gemma-evals-status
gemma-evals-check
gemma-examples-check
git status --short
git diff -- docs/integrations/space-agent prompts/opencode/phase7e1-space-agent-local-gemma-retry.prompt.txt

Acceptance criteria:
- Local Gemma retry result is documented without secrets.
- Gemini retry result is documented without secrets.
- OpenRouter working state remains documented.
- No ~/conf/onscreen-agent.yaml direct edit was made.
- No Space Agent autonomous task was run.
- No system/security/package/model/OpenCode/Agent Zero/Space Agent config was changed.
