Phase 6C: Space Agent Sandbox Prompt

Context:
You are working on the Bazzite Local AI Operations Stack at ~/projects/gem. Phase 5 (A-F) is complete. Phase 6C tests Space Agent Linux AppImage in user space.

Purpose:
Test Space Agent AppImage to verify:
- Downloads and runs on Bazzite
- Config/state paths identified
- Local inference options available
- No autostart or broad filesystem access needed

Current Known State:
- Space Agent assessment complete (Linux compatible, no blockers)
- AppImage available at: https://github.com/agent0ai/space-agent/releases/tag/v0.64

Hard Boundaries:
- Do NOT leave Space Agent running
- Do NOT create systemd service or autostart
- Do NOT install to system
- Do NOT grant broad filesystem access beyond workspace
- Do NOT configure for production use
- Do NOT change OpenCode permissions
- Do NOT modify Ollama service or model config

Allowed Actions:
1. Query GitHub releases API for latest version
2. Verify AppImage URL exists (curl -I) before downloading
3. Download AppImage to temp location (only if URL verified)
4. Make executable and run briefly (timeout)
5. Identify created config paths
6. Check for local inference options in docs
7. Clean up test directory
8. Document findings

Validation Commands:
```bash
# STEP 1: Query GitHub API for latest release
curl -s https://api.github.com/repos/agent0ai/space-agent/releases/latest | grep -E '"tag_name"|"name"' | head -2

# STEP 2: Get download URLs from latest release (extract linux-x64 AppImage)
curl -s https://api.github.com/repos/agent0ai/space-agent/releases/latest | grep -o '"browser_download_url": *"[^"]*linux-x64\.AppImage"' | cut -d'"' -f4

# STEP 3: Verify URL exists before downloading (curl -I)
# Replace with actual URL from step 2
curl -I -L "https://github.com/agent0ai/space-agent/releases/download/v0.64/Space-Agent-0.64-linux-x64.AppImage" 2>&1 | head -5

# STEP 4: Only if URL verified, download
mkdir -p ~/tmp/space-agent-test
cd ~/tmp/space-agent-test
curl -L -o Space-Agent-AppImage "VERIFIED_URL_FROM_STEP_2"

# STEP 5: Make executable
chmod +x Space-Agent-AppImage

# STEP 6: Test run (brief, don't leave running)
timeout 5 ./Space-Agent-AppImage --help 2>/dev/null || timeout 5 ./Space-Agent-AppImage 2>&1 | head -10 || echo "AppImage exited"

# STEP 7: Check created directories (may not exist if --help only)
ls -la ~/.config/space-agent/ 2>/dev/null || echo "Not created"
ls -la ~/.local/share/space-agent/ 2>/dev/null || echo "Not created"

# STEP 8: Clean up (LEAVE no temp files for Phase 6D to check)
rm -rf ~/tmp/space-agent-test
```

Output Format:
Create docs/integrations/space-agent/SPACE_AGENT_SANDBOX_REPORT.md with:
- AppImage download confirmation
- Config path identification
- Launch test results
- Local inference options status
- Recommendations for Phase 7C

Final Response:
- Confirm download/test performed
- Report any issues found
- State whether ready for Phase 7C integration
