Prompt: Phase 12L — Space Agent Install and Manual UI Provider Dry-Run

Purpose:
Install Space Agent using the method approved in Phase 12K, then perform a read-only manual UI provider dry-run without exposing secrets or modifying configs.

Context:
- Phase 12K has assessed installation readiness
- Space Agent is NOT currently installed
- Installation method: [to be determined by Phase 12K]
- Host: Bazzite/Fedora Atomic Desktop

Prerequisites (ALL must be true):
- [ ] Phase 12K complete with APPROVED_FOR_INSTALL decision
- [ ] Human explicitly approves installation AND dry-run
- [ ] Installation method documented and reviewed
- [ ] Rollback plan understood (how to uninstall if needed)

Hard Boundaries:
- Do NOT use sudo unless the approved install method explicitly requires it AND human approves.
- Do NOT install via rpm-ostree unless explicitly approved.
- Do NOT modify firewall rules.
- Do NOT start Space Agent as a daemon or service.
- Do NOT expose Space Agent on LAN.
- Do NOT edit ~/conf/onscreen-agent.yaml from repo.
- Do NOT store secrets in repo.

Allowed:
- Install Space Agent using approved user-local method
- Launch Space Agent temporarily for UI inspection
- Inspect provider settings in UI (manual only)
- Document findings
- Stop Space Agent after dry-run
- Create dry-run report

Installation Steps (Template):

1. Download/install using approved method
2. Verify binary/container is present
3. Launch Space Agent temporarily
4. Wait for UI to initialize
5. Inspect provider settings (manual user entry)
6. Test connectivity to local Ollama (if provider configured)
7. Document results
8. Stop Space Agent

Dry-Run Checklist:

- [ ] Space Agent launches successfully
- [ ] UI is accessible
- [ ] Provider settings visible
- [ ] Local Ollama endpoint can be configured
- [ ] No secrets exposed in output
- [ ] No system configs modified
- [ ] Space Agent stopped after dry-run

Deliverables:
- docs/phase12/PHASE12L_SPACE_AGENT_INSTALL_AND_MANUAL_UI_DRY_RUN.md
- Dry-run report: ~/offload/security-reports/manual/phase12l-space-agent-dry-run-YYYYMMDD-HHMMSS.md
- Installation decision:
  - SUCCESS: Space Agent installed and operational for manual use
  - PARTIAL: Installed but provider/Ollama issues remain
  - FAILED: Could not install or launch safely

Expected Output:
- Installation steps used
- Launch verification
- UI inspection results
- Provider configuration notes (no secrets)
- Stop verification
- Next steps

Note:
This phase requires EXPLICIT HUMAN APPROVAL for installation.
Do not proceed if Phase 12K was BLOCKED.
