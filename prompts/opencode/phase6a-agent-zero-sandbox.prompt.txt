Phase 6A: Agent Zero Sandbox Prompt

Context:
You are working on the Bazzite Local AI Operations Stack at ~/projects/gem. Phase 5 (A-F) is complete. Phase 6A tests Agent Zero in isolated sandbox.

Purpose:
Test Agent Zero container in isolated environment to verify:
- Container starts correctly
- Config paths are valid
- A0 CLI connector availability
- Sandbox isolation boundaries

Current Known State:
- Agent Zero container image exists (docker.io/agent0ai/agent-zero:latest)
- Container is not running (exited)
- Config directories exist: ~/.config/agent-zero/, ~/.local/share/agent-zero/
- Helper scripts exist: agent-zero-up, agent-zero-down, agent-zero-status

Hard Boundaries:
- Do NOT use sudo
- Do NOT install packages to system
- Do NOT start Agent Zero service in this prompt (verification only)
- Do NOT modify container config
- Do NOT grant additional permissions to container
- Do NOT expose container to network beyond localhost
- Do NOT make system changes
- Do NOT modify Ollama service or model config
- Do NOT change OpenCode permissions

Allowed Actions:
1. Check container status (podman ps -a)
2. Inspect container config (podman inspect)
3. Verify config paths (ls -la)
4. Check for A0 CLI binary (which, ls)
5. Verify endpoint if container running (curl)
6. Document findings in report

Validation Commands:
```bash
# Container status
podman ps -a | grep agent-zero

# Config paths
ls -la ~/.config/agent-zero/
ls -la ~/.local/share/agent-zero/

# CLI availability
which a0
ls -la ~/.local/bin/a0 2>/dev/null || echo "Not found"

# Endpoint test (if running)
curl -s http://127.0.0.1:5080 || echo "Not reachable"
```

Output Format:
Create docs/integrations/agent-zero/AGENT_ZERO_SANDBOX_REPORT.md with:
- Container status
- Config path verification
- CLI availability
- Isolation confirmation
- Recommendations for Phase 7A

Final Response:
- Confirm all checks performed
- Report any issues found
- State whether ready for Phase 7A integration
