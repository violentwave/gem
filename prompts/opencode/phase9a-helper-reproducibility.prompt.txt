Phase 9A: Helper Source Reproducibility Planning

Goal:
Plan sanitized repo-tracked source templates and user-local install/check flows for `gemma-memory-search` and `gemma-memory-rag`.

This is planning-only.

Must cover:
- proposed repo paths under `helpers/`
- `scripts/install-gemma-memory-helpers.sh`
- `scripts/check-gemma-memory-helpers.sh`
- no blind copy from `~/.local/bin/`
- no secrets/runtime state/reports/logs committed
- dry-run mode
- checksum comparison
- safe overwrite confirmation
- copy only to `~/.local/bin/`
- executable-bit validation
- `python3 -m py_compile` where applicable

Hard boundaries:
- no sudo
- no package installs
- no live helper replacement without explicit future approval
- no wrapper default changes

Outputs:
- update/create `docs/phase9/HELPER_REPRODUCIBILITY_PLAN.md`

Validation:
- `test -f docs/phase9/HELPER_REPRODUCIBILITY_PLAN.md`

Final response format:
- files created
- files modified
- installer/checker behavior planned
- boundaries preserved
- readiness decision
