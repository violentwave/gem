Phase 8C: Space Agent Workspace Workflow Library

You are working on Logan's Bazzite Local AI Operations Stack repo:

~/projects/gem

Goal:
Create the workspace workflow library for L7 operations using Space Agent as manual UI/workspace layer.

Current state:
- Phase 7E.1 complete: Space Agent works with OpenRouter and local Gemma/Ollama
- Endpoint: http://127.0.0.1:11434/v1/chat/completions
- Model: gemma4-e4b-bazzite:latest
- API key placeholder: ollama
- Gemini optional unresolved retry
- Space Agent is manual UI only, no repo editing

Component roles:
- Space Agent: manual UI/workspace layer only
- OpenCode: implementation work, repo edits
- Gemma wrappers: advisory, RAG
- Agent Zero: future orchestration (not active)
- Human: approval authority

Hard boundaries:
- Do not let Space Agent edit ~/projects/gem while OpenCode is active
- Do not grant Space Agent broad filesystem access
- Do not run Space Agent autonomous tasks
- Do not create or edit ~/conf/onscreen-agent.yaml
- Do not modify Space Agent config

Tasks:

1. Review Space Agent status:
   - docs/integrations/space-agent/SPACE_AGENT_PHASE7E1_PROVIDER_FINALIZATION.md
   - Check: provider working, role defined

2. Create workspace workflow library:
   - docs/workflows/space-agent/WORKSPACE_WORKFLOW_LIBRARY.md

3. Define workspace workflows:
   - Manual provider testing (OpenRouter, local Gemma/Ollama)
   - Context display for human review
   - Task results presentation
   - Workspace state visualization

4. For each workflow document:
   - Purpose and use case
   - Inputs and scope
   - Allowed actions (manual UI only)
   - Forbidden actions (no autonomous tasks, no repo edits)
   - Fallback path (terminal + Gemma)
   - Stop conditions

5. Include routing rules:
   - Space Agent: manual-only, user-initiated
   - No autonomous Space Agent tasks
   - OpenCode remains implementation surface

Validation:
Run from ~/projects/gem:

test -f docs/workflows/space-agent/WORKSPACE_WORKFLOW_LIBRARY.md
gemma-evals-status
gemma-evals-check
gemma-examples-check

Acceptance criteria:
- Workspace workflow library exists
- Manual-only role preserved
- No autonomous tasks
- No repo editing through Space Agent
- Validators pass

Final response format:
- files created
- workflows defined
- manual-only confirmed
- boundaries preserved
- validators run
- readiness decision
