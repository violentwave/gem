Prompt: Phase 12C — Space Agent Manual UI Bridge Dry-Run Review

Purpose:
Review whether Space Agent can be used as a manual display/orchestration UI without starting Space Agent by default, modifying Space Agent config, editing ~/conf/onscreen-agent.yaml, or granting repo/host write access.

Context:
- Space Agent is the L7 manual UI/workspace layer.
- Space Agent config is at ~/conf/onscreen-agent.yaml (real file, not repo-managed).
- Space Agent providers: OpenRouter, local Gemma/Ollama, Gemini native.
- Space Agent is NOT approved for autonomous tasks, repo edits, or system commands.
- Previous assessment (Phase 7E.1) confirmed OpenRouter and local Gemma work in Space Agent UI.
- Current stack: OpenCode (primary implementation), Gemma (advisory), Stage 3A (canonical fallback), RuVector (supervised prototype).

Hard Boundaries:
- Do not start Space Agent by default.
- Do not modify Space Agent config.
- Do not edit ~/conf/onscreen-agent.yaml.
- Do not grant repo write authority to Space Agent.
- Do not grant host write authority to Space Agent.
- Do not enable autonomous tasks.
- Do not run OpenCode prompts autonomously.
- Do not ingest, index, mutate, or promote memory.
- Do not enable learning/training.
- Do not use sudo.
- Do not modify system/security configs.

Safe Workflow:
If explicitly authorized to use Space Agent for a manual UI dry-run:
1. Open Space Agent UI manually (human action).
2. Configure provider to local Gemma/Ollama (human action).
3. Define a single read-only workflow (e.g., display checklist).
4. Run workflow manually with human supervision.
5. Review output for boundary compliance.
6. Do not save workflow as autonomous task.

Stop Conditions:
- Space Agent requests file write access.
- Space Agent requests system commands.
- Space Agent requests memory mutation.
- Space Agent requests learning/training execution.
- Output contains suspicious or out-of-bounds recommendations.
- Human reviewer is not available.

Expected Output:
- Manual workflow display
- Boundary compliance confirmation
- Human approval points documented

Note:
This prompt is for future explicit authorization only. Do not execute Space Agent without explicit human approval.