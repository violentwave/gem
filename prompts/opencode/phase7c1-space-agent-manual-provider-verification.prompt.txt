Phase 7C.1: Space Agent Manual Provider Verification Prompt

You are working on Logan's Bazzite Local AI Operations Stack repo:

~/projects/gem

Goal:
Guide and document a manual Space Agent provider verification pass without exposing secrets or allowing repo edits.

Current state:
- Phase 7C report exists: `docs/integrations/space-agent/SPACE_AGENT_PHASE7C_WORKSPACE_AND_MODELS_REPORT.md`.
- Space Agent runs manually as an AppImage.
- Config path is `~/.config/space-agent/`.
- Provider guidance is documented, but manual UI test remains pending.

Hard boundaries:
- Do not print API keys or provider secrets.
- Do not inspect cookies, Local Storage, Session Storage, Trust Tokens, or secret files.
- Do not let Space Agent edit `~/projects/gem`.
- Do not grant broad filesystem access.
- Do not run autonomous Space Agent tasks against the host.
- Do not start Agent Zero.
- Do not modify Ollama, OpenCode, firewall, systemd, package, or model config.

Manual settings to verify:

Google Gemini native OpenAI-compatible provider:
- Endpoint: `https://generativelanguage.googleapis.com/v1beta/openai/`
- Model: `gemini-2.5-pro` or `gemini-2.5-flash`
- Do not use `google/gemini-2.5-pro` with this endpoint.
- Initial max tokens: `8192` or `16384`.

Local Ollama/Gemma provider:
- Endpoint: `http://127.0.0.1:11434/v1` or native `http://127.0.0.1:11434` if using a native provider mode.
- Model: `gemma4-e4b-bazzite` or `gemma4-e4b-bazzite:latest`.
- API key: blank or `ollama`, depending on UI requirement.
- Do not use provider-prefixed model names.

Tasks:
1. Re-read the Phase 7C report.
2. Ask the user to manually enter provider secrets in Space Agent UI if needed.
3. Run only tiny manual provider tests.
4. Record success/failure without printing secrets.
5. Create `docs/integrations/space-agent/SPACE_AGENT_PHASE7C1_MANUAL_PROVIDER_VERIFICATION.md`.
6. Keep Space Agent as manual exploration only.

Acceptance criteria:
- No secrets printed or copied.
- No repo edits by Space Agent.
- No broad filesystem access granted.
- Provider settings are verified or blocked with clear reason.
- Existing validators pass.

Final response format:
- provider settings tested
- pass/fail results
- blocked items
- files created/modified
- readiness decision
